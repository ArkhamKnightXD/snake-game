package knight.arkham.helpers;

import knight.arkham.SnakeGame;

public class Constants {

    public static final float PIXELS_PER_METER = 32.0f;
    public static final int FULL_SCREEN_HEIGHT = SnakeGame.INSTANCE.getScreenHeight();
    public static final int FULL_SCREEN_WIDTH = SnakeGame.INSTANCE.getScreenWidth();
    public static final int MID_SCREEN_HEIGHT = SnakeGame.INSTANCE.getScreenHeight() / 2;
    public static final int MID_SCREEN_WIDTH = SnakeGame.INSTANCE.getScreenWidth() / 2;
}
